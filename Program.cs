// See https://aka.ms/new-console-template for more information
using System;

namespace Aquino
{
	class activity{
		static void Main (string[] args)
		{
			string [] weeks = new[] {"Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday",};
			
			for (int i = 0; i < weeks.Length; i++);
			console.writeLine(weeks[i].ToUpper());
		}
	}
}